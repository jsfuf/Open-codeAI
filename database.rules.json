{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "Opencode AI": {
      ".read": false,
      ".write": false
    }
  }
}
